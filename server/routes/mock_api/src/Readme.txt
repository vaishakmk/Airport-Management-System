1. Run: npm install json-server --save

2. Run: json-server --watch src/db.json --port 4000

3. Access the contents of the db through a GET request. Via command line, you can just do:

http://localhost:4000/flights